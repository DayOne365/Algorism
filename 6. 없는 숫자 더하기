class Solution {
    public int solution(int[] numbers) {
        // 1 ~ 9 까지의 합인 45에서 numbers의 합을 빼주도록 한다.
        int answer = 45;
        
        for(int number : numbers) { // 변수 : 배열 배열에있는 값을 하나하나 변수에 담는다.
            answer -= number;
        }    
        
        return answer;
    }
}
